{
  "schema_version": 1,
  "kind": "selective_replan_against_full_teacher_confirmation",
  "question": "Does the frozen disagreement-triggered selective-search controller outperform the frozen routed-v6 direct policy against the SAME full replanned three-turn search opponent on fresh two-player maps, while querying the teacher on a minority of its own decisions?",
  "frozen_controller": "Exactly the seed6530000 gate controller: rejected Markov student SHA1d6e6530bb2f16d273910fdeb4b2de491d2e115017468eb3f31d3aea43f64fe4 and routed-v6 source SHA68549a5af87e4d2d065a164e39bc515c2c1d48276f437494d757e25c3ef28867. At candidate states use source action on agreement, otherwise query native root256/top8/opponent64/followup32 replan_each_action=true and use its legal action. No threshold, checkpoint or search-budget changes.",
  "arena": "Fresh64 independent Classic Generic procedural_v2 11x9 two-player maps seed6531000, both candidate seats, action limit2400. Frozen full replanned three-turn search is the opponent in BOTH candidate and direct-source reports. Each report must use the identical map/seat schedule, rules, opponent parameters and self-play reference.",
  "smoke": "Use exactly2 non-gate maps seed6530900 to verify that the candidate uses its separate frozen source checkpoint for disagreement detection while the baseline opponent is full replanned search. Verify no search is called on candidate agreement, all moves legal, per-game censoring and query accounting. Exclude smoke outcomes from every gate.",
  "comparison": "Play the selective controller and the direct frozen source separately against the same full teacher, 128 games per candidate. Require 256 compared games terminal and zero censoring, positive selective-minus-source wins in each own seat, map-grouped exact two-sided sign p<0.05, and positive lower bound of map-bootstrap95 pool-relative Elo. Report actual candidate and source win counts, independent-map paired outcomes, both seats, opponent self-play ledger and selective teacher-query fraction at most50%. No global Elo claim.",
  "runtime": "If the game gate passes, predeclare and run a separate matched one-core CPU per-action/per-turn latency comparison on fresh disjoint16 maps seed6532000, same profiles and controllers, with median and p95 and identical sampled states where feasible. Teacher-query fraction is not a latency measurement. Only after runtime and fresh Online Default/Classic Slay profile gates consider owner-private opt-in Site release; rated bot unchanged.",
  "stop_rule": "If the fresh fixed-teacher game gate fails, keep the source and full teacher unchanged, reject selective-controller promotion and do not tune on seed6531000 or run reserved runtime/profile windows. The rejected student remains rejected as a standalone policy even if the search-assisted controller passes.",
  "resources": "Heavy compute only on SSH server fedora-toolbox scratch, one pinned CPU15 nice19 with Rayon/BLAS caps and bounded sequential jobs, tiny reports, no GPU or concurrent heavy process. Avoid other user workloads and preserve original Antiyoy mechanics, owner-private access and current rated/browser agents."
}
